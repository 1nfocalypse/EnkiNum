# allow for creation with set key AND use w/ vernam OTP
def viginere(cleartext, key, cbool):
    """
    An implementation of a Viginere cipher.

    :param cleartext: string of unencrypted text
    :param key: key for encryption, decryption
    :param cbool: 0 for encryption, 1 for decryption
    :return: string (ciphertext)
    """

    if (cbool == 0):

    else:
